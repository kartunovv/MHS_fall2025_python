[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "latexgen"
version = "0.1.0"
description = "Functional LaTeX generator (tables and images)"
authors = [{ name = "Kartunov Vlad" }]
dependencies = []
requires-python = ">=3.10"

[tool.setuptools]
packages = ["latexgen"]